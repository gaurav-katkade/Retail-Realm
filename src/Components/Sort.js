import styled from "styled-components";

const Sort =()=>{
    return<Wrapper>

    </Wrapper>;
};
const Wrapper = styled.section`
    height:10rem;
    background-color:yellow;
`;
export default Sort;